{
  "phases": {
    "install": {
      "cmds": ["npm install --production=false"]
    },
    "build": {
      "cmds": ["npm run build"]
    }
  },
  "start": {
    "cmd": "NODE_ENV=production node dist/index.cjs"
  }
}
